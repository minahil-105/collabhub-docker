server {
    listen 80;

    # All /api/* requests go to the API service
    location /api/ {
        proxy_pass http://api:5000;
    }

    # Everything else goes to the web service
    location / {
        proxy_pass http://web:3000;
    }
}
